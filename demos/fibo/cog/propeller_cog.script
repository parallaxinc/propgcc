SECTIONS
{
  .boot 0x0: { *(.boot) }
  .text 0x0 : AT (ADDR(.boot)+SIZEOF(.boot))
          {
	   *(.text*) 
	  }
  __hub_base = SIZEOF(.boot)+SIZEOF(.text);
  .data (__hub_base) : AT (__hub_base)
          {
            *(.data*)
          }
  .bss : { *(.bss*) }
}
